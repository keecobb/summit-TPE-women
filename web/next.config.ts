import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing custom yet -- keep this as the one place to add image domains,
  // redirects, etc. as the site grows.
};

export default nextConfig;
